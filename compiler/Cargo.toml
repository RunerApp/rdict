[package]
name = "rdict-compiler"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "GPL-3.0"

[dependencies]
rdict = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[[bin]]
name = "rdict-compile"
path = "src/main.rs"

[workspace]
